# Anthropic API key — used when the harness calls Claude directly for intake parsing.
ANTHROPIC_API_KEY=

# HomesToCompare connector — for live listing search and comparison creation.
H2C_BASE_URL=
H2C_API_KEY=
H2C_READ_KEY=

# Local demo configuration (defaults shown — override to point at your own data).
LISTINGS_DATA_PATH=evals/datasets/listings_small.jsonl
TRACE_OUTPUT_DIR=.traces
